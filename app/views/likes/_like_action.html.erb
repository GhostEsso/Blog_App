<%= form_with model: @like, url: user_post_likes_path(@post.author, @post), data: { turbo: false }, local: true do |f| %>
  <div>
    <%= f.hidden_field :post_id, value: @post.id %>
    <%= f.submit "Like this post" %>
  </div>
<% end %>
